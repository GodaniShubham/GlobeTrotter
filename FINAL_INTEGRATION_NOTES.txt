GlobeTrotter final integrated build

Base:
  Supplied local project preserved.

Integrated/verified:
  - GitHub main admin_panel code and templates are present.
  - Admin panel route is /admin-panel/.
  - Admin user/city/activity/analytics/settings screens remain.
  - Community feed + post/reply/like/save/share/notification routes are wired.
  - Itinerary routes and trip-specific compatibility routes are wired.
  - Auth pages are standalone so the app-shell merge cannot blank the sign-in/sign-up form.
  - Sidebar promo/profile card removed; Settings retained and Admin Panel link shown to superusers.
  - Missing admin user-detail template added.
  - Admin city/activity forms support local image upload or external image URL.
  - Existing migrations were preserved; no migration files were rewritten.
  - .env is intentionally excluded from this deliverable. Use .env.example.

Before first run on a clean checkout:
  python manage.py migrate
  python manage.py check
  python manage.py runserver
